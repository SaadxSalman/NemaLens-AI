import { LoaderCircle, Paperclip, ShieldCheck, Sparkles } from "lucide-react";

export function QueryComposer({ question, onQuestionChange, onFileChange, uploadedFile, isRunning, onRun }: { question: string; onQuestionChange: (value: string) => void; onFileChange: (file: File | undefined) => void; uploadedFile: string | null; isRunning: boolean; onRun: () => void }) {
  return <section className="query-panel panel"><div className="panel-heading"><div><span className="step">01</span><div><h2>Describe your finding</h2><p>Add morphology, exposure, symptoms, or a clinical question.</p></div></div><span className="mode-tag"><ShieldCheck size={13} />Private workspace</span></div><textarea value={question} onChange={(event) => onQuestionChange(event.target.value)} aria-label="Diagnostic question" /><div className="query-footer"><label className="attach-button"><Paperclip size={16} />{uploadedFile || "Attach microscopy"}<input type="file" accept="image/*,.pdf" onChange={(event) => onFileChange(event.target.files?.[0])} /></label><span className="char-count">{question.length} / 2,000</span><button className="run-button" onClick={onRun} disabled={isRunning}>{isRunning ? <><LoaderCircle size={16} className="spin" />Retrieving</> : <><Sparkles size={16} />Run analysis <span>⌘ ↵</span></>}</button></div></section>;
}
